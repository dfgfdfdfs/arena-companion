# v2026.09.12 验证范围

- 读取通道：正常并发请求排队，真正挂起的原生请求不累积重复调用。
- 限流：使用实际 WebView2 原生响应事件，本地连续返回 `429 / Retry-After: 600`、`429 / Retry-After: 300`，第三次成功。两次请求间隔约 5.19 秒、5.14 秒；新的服务器时间替换旧时间。保留旧限流提示、延迟显示成功结果时也能继续，成功后不发生第四次提交。
- 控制器：手动暂停、草稿变化、附件检查、长回答、迟到的 Thinking、提交结果不明及成功后的候选检测。
- 图集：HTML 下载、完整渲染、保存、原会话对应、重命名、重复收集，以及同一文件同时出现工具入口和正文链接的情况。
- 会话恢复：严格核对缺失节点与当前会话，先落盘备份，状态发生变化时停止修复，不自动重发问题。
- 分发包：解压到新目录后验证首次密码设置与再次启动；仅包含程序、依赖、页面资源、说明和第三方许可。

本地模拟验证不代表网站会提前解除限流，也不保证网站未来的界面或接口保持兼容。
