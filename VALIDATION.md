# 验证范围

提交前运行以下本地验证：

- 主程序 Release x64 构建。
- 登录流程、控制器状态、限流识别、本机接口与任务设置测试。
- 候选 HTML 下载、完整截图、保存与对话对应测试。
- 会话缺失节点恢复与备份测试。
- 实例创建、重命名、删除及多实例隔离测试。
- IP 去重、顺序循环与网络状态格式化测试。
- 429 停止后只在 IP 切换确认成功时恢复一次，旧 429 记录不会重复拦截，普通暂停不会被误恢复。
- JavaScript 语法与本地 DOM 测试。
- 独立分发构建规则检查：不包含默认密码或运行账号资料。

本地夹具验证不能证明 Arena 网站未来的 DOM 或接口保持兼容，也不能替代真实账号下的人工验收。
